/* Generated by tools/build.mjs — do not edit. Source: content/react.html */
(window.GN_SECTIONS = window.GN_SECTIONS || []).push(
{"slug":"react","topic":"what","note":"React","noteTitle":"React","section":"What React is","text":"React is a library for building what a person sees in a browser. Its one big idea is worth more than every feature it has: you never describe how to change the screen, you describe what the screen should look like for some data . React works out the changes. In plain English Old way: “find the price label, erase it, write 40 in it, then find the total, erase that too…” React way: “the cart has these items; here is what a cart with these items looks like.” When an item is added you hand React the new list and it works out which words on the screen actually changed. That is the whole trade. You give up direct control of the page, and you get back the thing that makes large screens survivable: "},
{"slug":"react","topic":"components","note":"React","noteTitle":"React","section":"Components & JSX","text":"In plain English A component is a recipe. Save button is written once, and then used wherever a save button belongs — the way a recipe for bread is written once and baked a hundred times. What makes it more than copy-and-paste is that changing the recipe changes every loaf: fix the button in one place and it is fixed on every screen that uses it. A component is a function that returns a description of some screen. That is genuinely all it is: a function whose name starts with a capital letter and which returns markup. js A component The markup inside the function is JSX . It looks like HTML and is not: it is JavaScript that a build step turns into function calls. The differences that bite: H"},
{"slug":"react","topic":"props","note":"React","noteTitle":"React","section":"Props: data passed in","text":"In plain English Props are the arguments you hand a function, and they work exactly like a job order given to a workshop. The order says a blue chair, seat height 45cm ; the workshop builds to the order and does not get to change it. If a different chair is wanted, a new order is sent — the workshop never edits the one it was given. A component takes arguments, called props . They arrive as one object, and are almost always destructured in the parameter list so the names are visible at the top of the function. js Props in, markup out Two rules make props predictable, and both are non-negotiable: Props are read-only. A component never writes to its own props. If a value needs to change, it be"},
{"slug":"react","topic":"state","note":"React","noteTitle":"React","section":"State: data that changes","text":"Props come from outside. State is a value a component owns and can change — and changing it is what makes React redraw. useState gives you a value and a function to replace it. tsx useState In plain English useState hands you a value and a pen. Writing with the pen does not edit the screen directly; it tells React “this is different now”, React runs your function again with the new value, and the new screen falls out of that. Two things about setting state surprise everybody once: It is not immediate. Reading count on the line after setCount gives you the old value. The variable belongs to this run of the function; the new value shows up in the next one. It replaces, it does not merge. Setti"},
{"slug":"react","topic":"lists","note":"React","noteTitle":"React","section":"Lists & keys","text":"In plain English A key is a name tag. When you redraw a list, React has to work out which items on the new list are the same items it drew before. With name tags it can see that Ada simply moved from third place to first. Without them it can only count positions — so it concludes that everything from position one onwards changed, and rebuilds rows that did not need touching. A list of data becomes a list of elements with map — the array method that turns each item into something else. Ninety per cent of React screens are this. js Rendering a list The key is not decoration. It is how React tells one row from another between redraws, so it can move a row rather than rebuild it. Get it wrong an"},
{"slug":"react","topic":"forms","note":"React","noteTitle":"React","section":"Events & forms","text":"In plain English There are two ways to run a form. Either the input box remembers what was typed and you ask it later — which is how plain HTML works — or you remember it and the box just displays what you are holding. React prefers the second: you hold the value, the box shows it back. It sounds like more work, and it is the reason you can validate as somebody types, disable the button until the form is valid, and fill fields in from elsewhere. An event handler is a function passed to a prop like onClick or onChange . Pass the function, do not call it: onClick={save} runs it when clicked, onClick={save()} runs it immediately during rendering, which is a classic and confusing bug. Inputs are"},
{"slug":"react","topic":"effects","note":"React","noteTitle":"React","section":"Effects: talking to the outside world","text":"In plain English Everything else in React happens inside React: data goes in, a description of the screen comes out. An effect is the door to the outside world — fetching from a server, starting a timer, listening to the browser window. And whatever you start when the door opens, you have to stop when it closes; that is what the cleanup function is for, and forgetting it is where most effect bugs come from. Rendering must be pure: given the same props and state, a component returns the same markup and touches nothing else. Everything that breaks that rule — fetching, timers, subscriptions, listening to the browser — is a side effect , and goes in useEffect . tsx Fetching when the screen appe"},
{"slug":"react","topic":"hooks","note":"React","noteTitle":"React","section":"The other hooks worth knowing","text":"In plain English A hook is a function starting with use that lets a component borrow one of React s abilities — remembering a value, reaching outside, holding on to something between renders. There are only a handful worth knowing, and the rest of the list can wait until you have a problem that names itself. A hook is a function starting with use that lets a component remember something between renders. Two rules apply to every one of them, and breaking either produces baffling bugs rather than errors: Only at the top level. Never inside an if , a loop or a nested function. React matches hooks to their stored values by call order, so a hook that sometimes runs shifts every hook after it onto"},
{"slug":"react","topic":"sharing","note":"React","noteTitle":"React","section":"Sharing state between components","text":"In plain English Two components need to agree about one fact. The answer is not to give each of them a copy — two copies drift apart. It is to decide who owns it. Move the fact up to the nearest component that contains them both, and let it hand the value down. One owner, one truth, and no way for the two to disagree. When two components need the same value, the value moves up to their closest common parent, which passes it down to both. This is called lifting state up, and it is the default answer. tsx Lifting state up It stops being the answer when the value is needed by something eight levels down and every level in between is only passing it along. That is context : a value published at "},
{"slug":"react","topic":"mistakes","note":"React","noteTitle":"React","section":"The mistakes everybody makes","text":"In plain English None of these are exotic. They are the four or five things that every person learning React gets wrong, including people who have been writing it for years. Knowing them by name is most of the cure, because each one has a distinctive symptom — and recognising the symptom takes you straight to the cause instead of an afternoon of guessing. Symptom Cause Fix The screen does not update, though the data changed. State was edited in place, so it is still the same object. Replace it: setItems([...items, x]) . \"Too many re-renders\" and the tab freezes. A handler was called instead of passed — onClick={save()} — or an effect sets state it also depends on. Pass the function: onClick="},
{"slug":"react","topic":"here","note":"React","noteTitle":"React","section":"React in this product","text":"In plain English Everything above was the idea. This is where it lands in a real codebase — which files, doing which job. Reading the note and then reading the actual code is worth far more than reading the note twice: the second example you meet should always be a real one. Every screen you are looking at right now is React. Some places worth opening once you have the ideas above, in increasing order of difficulty: components/admin/GuideDocument.tsx — small components, props and children , plus one useEffect that watches which section is on screen. Nothing else. app/contexts/AuthContext.tsx — the context that holds who is signed in. Every page that greets you by name reads it. components/sy"}
);
